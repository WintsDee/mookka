
import { Media, User } from '@/types';

// Mock des données utilisateur (vidé)
export const currentUser: User = {
  id: '',
  name: '',
  avatar: '',
  bio: '',
  following: 0,
  followers: 0
};

// Mock des données médias (vidé)
export const mockMedia: Media[] = [];

// Mock des actualités (vidé)
export const mockNews = [];

// Mock des activités sociales (vidé)
export const mockSocial = [];
